# Monad Demo

This project is to showcase some common types and their Monad instances and
behavior.  Currently, only State is shown which is a bit more interesting than
the more common types used to demonstrate Monads like Maybe and IO.
